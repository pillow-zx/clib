/**
 * @file compiler.h
 * @brief 编译器抽象层入口，聚合属性封装与内建函数封装两个子模块。
 *
 * 需要编译器辅助宏时统一包含此头文件即可。
 */
#ifndef CLIB_COMPILER_H
#define CLIB_COMPILER_H

#include <compiler/compiler-attribute.h>
#include <compiler/compiler-builtin.h>

#endif
